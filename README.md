## Dependencies

Treesitter `0.26.5` (install using `cargo`)
```bash
cargo install --locked tree-sitter-cli
```

`golang` (for `efm-langserver`)
```bash
sudo dnf -S golang
```

Other general dependencies:
```bash
sudo dnf -S git ripgrep fzf fd
```
